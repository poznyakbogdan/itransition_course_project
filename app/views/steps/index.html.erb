<div class="container">
	
	<%= render 'show_editable_steps', :instruction_id => @instruction_id %>

	<%= link_to "Save instruction", save_steps_numbers_path(@instruction_id), remote: true,  
														:id => "steps-save-link" %>
</div>

